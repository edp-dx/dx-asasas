apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "asasas.fullname" . }}-test-connection"
  labels:
    {{- include "asasas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "asasas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
