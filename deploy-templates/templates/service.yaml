apiVersion: v1
kind: Service
metadata:
  name: {{ include "asasas.fullname" . }}
  labels:
    {{- include "asasas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "asasas.selectorLabels" . | nindent 4 }}
